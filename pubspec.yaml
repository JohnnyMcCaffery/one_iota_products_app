name: one_iota_products_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  badges: ^3.1.2
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  go_router: ^14.2.1
  http: ^1.2.2
  json_annotation: ^4.9.0
  money2: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
